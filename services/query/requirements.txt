# ============================================================================
# FILE: services/query/requirements.txt
# ARCHITECTURE REF: §4 — RAG Query Pipeline
# ============================================================================

fastapi==0.111.0
uvicorn[standard]==0.29.0
pydantic-settings==2.3.0

# HTTP clients — all external service calls
httpx==0.27.0

# Redis client — for semantic cache
redis==5.2.1

# Qdrant client — for hybrid vector search
qdrant-client>=1.12.0,<1.13.0

# Numpy — for cosine similarity computation in semantic cache
numpy==1.26.4

# JWT validation
python-jose[cryptography]==3.3.0

# Prometheus metrics
prometheus-fastapi-instrumentator==6.1.0

# Retry with exponential backoff
tenacity==8.3.0

# SSE streaming response support
sse-starlette==2.1.0
